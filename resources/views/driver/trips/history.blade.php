<x-app-layout>
    @php $badge = ['Completed' => 'bg-gray-100 text-gray-600', 'Cancelled' => 'bg-red-50 text-red-700']; @endphp
    <div class="min-h-screen bg-[#F8FAFC] px-4 py-8 sm:px-6 lg:px-8"><div class="mx-auto max-w-7xl"><div class="mb-8 flex items-center gap-3"><a href="{{ route('driver.trips.index') }}" class="rounded-xl p-2 text-gray-400 hover:bg-gray-100 hover:text-gray-700">←</a><h1 class="text-2xl font-bold text-gray-900">Trip History</h1></div><x-trip-toast />
        <form method="GET" class="mb-6 flex flex-col gap-3 lg:flex-row"><input name="search" value="{{ request('search') }}" placeholder="Search by route or vehicle..." class="min-w-0 flex-1 rounded-xl border-gray-200 bg-white px-4 py-2.5 text-sm focus:border-[#16A34A] focus:ring-[#16A34A]"><select name="status" class="rounded-xl border-gray-200 bg-white text-sm text-gray-700"><option value="">All Status</option><option value="Completed" @selected(request('status') === 'Completed')>Completed</option><option value="Cancelled" @selected(request('status') === 'Cancelled')>Cancelled</option></select><input type="date" name="date" value="{{ request('date') }}" class="rounded-xl border-gray-200 bg-white text-sm text-gray-700"><button class="rounded-xl border border-gray-200 bg-white px-4 py-2 text-sm font-medium text-gray-700 hover:bg-gray-50">Apply</button></form>
        <div class="overflow-hidden rounded-2xl border border-gray-100 bg-white">@if($trips->isEmpty())<div class="py-16 text-center text-sm text-gray-500">There are no trip history records to display.</div>@else<div class="overflow-x-auto"><table class="min-w-full"><thead><tr class="border-b border-gray-100">@foreach(['Date','Route','Vehicle','Passengers','Distance','Duration','Income (RM)','Status'] as $heading)<th class="px-4 py-3 text-left text-xs font-semibold uppercase tracking-wide text-gray-500">{{ $heading }}</th>@endforeach</tr></thead><tbody class="divide-y divide-gray-50">@foreach($trips as $trip)<tr class="cursor-pointer hover:bg-gray-50" onclick="window.location='{{ route('driver.trips.show', ['trip' => $trip, 'return_to' => 'driver.trips.history']) }}'"><td class="whitespace-nowrap px-4 py-3 text-sm text-gray-700">{{ $trip->departure_at->format('d M Y') }}</td><td class="px-4 py-3 text-sm font-medium text-gray-900">{{ $trip->departure_location }} – {{ $trip->destination }}</td><td class="px-4 py-3 text-sm text-gray-700"><div>{{ $trip->vehicle->brand }} {{ $trip->vehicle->model }}</div><div class="font-mono text-xs text-gray-400">{{ $trip->vehicle->plate_number }}</div></td><td class="px-4 py-3 text-sm text-gray-700">0</td><td class="px-4 py-3 text-sm text-gray-700">{{ $trip->estimated_distance_km ? $trip->estimated_distance_km.' km' : '—' }}</td><td class="px-4 py-3 text-sm text-gray-700">{{ $trip->duration ?? '—' }}</td><td class="px-4 py-3 text-sm font-semibold text-[#16A34A]">RM 0.00</td><td class="px-4 py-3"><span class="inline-flex rounded-full px-2.5 py-1 text-xs font-medium {{ $badge[$trip->status] }}">{{ $trip->status }}</span></td></tr>@endforeach</tbody></table></div><div class="border-t border-gray-50 px-4 py-3">{{ $trips->links() }}</div>@endif</div></div></div>
</x-app-layout>
